<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sust.bookshelf">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.location" />
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bookcase"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bookcase"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.sust.bookshelf.UserManual"></activity>
        <activity android:name="com.sust.bookshelf.AllBooksActivity" />
        <activity android:name="com.sust.bookshelf.RequestForBookActivity" />
        <activity android:name="com.sust.bookshelf.LentBooks" />
        <activity android:name="com.sust.bookshelf.BorrowedBooks" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name="com.sust.bookshelf.RequestedBooksActivity" />
        <activity android:name="com.sust.bookshelf.RequestsActivity" />
        <activity android:name="com.sust.bookshelf.UpdateProfile" />
        <activity android:name="com.sust.bookshelf.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.sust.bookshelf.HomePageActivity" />
        <activity android:name="com.sust.bookshelf.SearchResults" />
        <activity android:name="com.sust.bookshelf.BookProfile" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name="com.sust.bookshelf.PostReview" />
        <activity android:name="com.sust.bookshelf.MapActivity" />
        <activity android:name="com.sust.bookshelf.Profile" />
        <activity android:name="com.sust.bookshelf.BookList" />
        <activity android:name="com.sust.bookshelf.WishList" />
        <activity android:name="com.sust.bookshelf.SignupActivity" />
    </application>

</manifest>